<%= form_with(model: medication, local: true) do |form| %>
  <% if medication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(medication.errors.count, "error") %> prohibited this medication from being saved:</h2>

      <ul>
      <% medication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :medication_name %>
  </div>

  <div class="field">
    <%= form.label :dose %>
    <%= form.text_field :dose, id: :medication_dose %>
  </div>

  <div class="field">
    <%= form.label :frequency %>
    <%= form.text_field :frequency, id: :medication_frequency %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
